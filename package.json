{
  "name": "@dimkl/events",
  "version": "1.3.0-0",
  "description": "Minimal events register library",
  "main": "dist/src/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build",
    "example:minimal": "ts-node examples/minimal.ts",
    "example:isolated": "ts-node examples/isolated-event-bus.ts",
    "example:implicit": "ts-node examples/implicit-event-attach.ts",
    "example:options": "ts-node examples/event-options.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
